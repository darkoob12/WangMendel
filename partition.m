%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%   CREATING FUZZY SETS ON EACH VARIABLE
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

A = zeros(7,3);
C = zeros(7,3);
for i = 0:6
    t = 10/6;
    A(i+1,:) = [(i-1)*t - 5,i*t - 5,(i+1)*t  - 5];
    t = 50/6;
    C(i+1,:) = [(i-1)*t,i*t,(i+1)*t];
end
B = A;

clear t;
